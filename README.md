# Countdown

A simple countdown timer for the command line and a Gtk GUI version.

![screenshot](./screenshot.png)

To compile it in Fedora:

```
sudo dnf install gtk4-devel
git clone https://github.com/jailop/countdown.git
cd countdown/src
make
```

To install it:

```
sudo make install
```
